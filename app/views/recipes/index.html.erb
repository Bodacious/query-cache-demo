<h1>All recipes published by <%= current_user.name %></h1>

<% if @user.recipes.exists? %>
  <%= render partial: "recipes/recipe",
             collection: @user.recipes,
             # Allows us to artificially control whether author is called from query cache or not
             locals: { user: memoized? ? @user : nil } %>
<% else %>
  <div class="empty-set"><%= @user.name %> hasn't published any recipes yet.</div>
<% end %>
